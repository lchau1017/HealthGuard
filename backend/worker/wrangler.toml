name = "medguard-extraction-proxy"
main = "src/index.ts"
compatibility_date = "2025-06-01"

# OPENROUTER_API_KEY is a secret:
#   local dev:  put it in .dev.vars (gitignored)
#   deployed:   npx wrangler secret put OPENROUTER_API_KEY
#
# MODEL_ID is optional; defaults to qwen/qwen2.5-vl-72b-instruct in code.
# [vars]
# MODEL_ID = "qwen/qwen2.5-vl-72b-instruct"
